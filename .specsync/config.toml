# spec-sync v4 configuration
specs_dir = "specs"
source_dirs = ["bin", "scripts"]
exclude_dirs = []
required_sections = ["Purpose", "Public API", "Invariants", "Behavioral Examples", "Error Cases", "Dependencies", "Change Log"]
enforcement = "strict"

[lifecycle]
track_history = false
